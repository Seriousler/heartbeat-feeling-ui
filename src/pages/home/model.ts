
import type { request } from '@/utils';
import type { Model } from '@/store';
import type { HomeModel } from './types'

export const indexModel: Model<HomeModel> = {
  namespace: 'Home',
  state: {},
  effects: {},
  reducers: {}
};
